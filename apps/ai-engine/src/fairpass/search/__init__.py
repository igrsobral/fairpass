"""Hybrid search: query understanding, Qdrant dense+sparse retrieval, business rerank.

Mirrors the schema-first contract from ``packages/api/src/types.ts`` so the
tRPC surface, the web UI, the matchmaking agent tool, and the eval harness all
agree on one shape.
"""